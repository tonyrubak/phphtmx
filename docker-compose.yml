version: '3'
services:
    web:
        image: nginx:latest
        ports:
            - "80:80"
        volumes:
            - ./default.conf:/etc/nginx/conf.d/default.conf
            - .:/app
    php:
        image: php:fpm
        volumes:
            - .:/app

